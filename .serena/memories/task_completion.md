After frontend changes, prefer running `npm run typecheck` and at least targeted Vitest cases for touched components/hooks. For broader changes, run `npm run test -- --run` if the environment allows it. For backend changes, run `go test ./...`. Be aware some existing frontend hook tests may rely on stale mocks and can fail independently of new changes; inspect failures before assuming regressions.